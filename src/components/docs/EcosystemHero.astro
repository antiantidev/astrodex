---
import { SITE_CONFIG } from '../../site.config';
import { getRelativeUrl } from '../../utils/url';
---

<div class="pt-24 md:pt-32 pb-16 md:pb-24 border-b border-border-subtle bg-midnight">
    <div class="container mx-auto px-6">
        <nav class="flex items-center gap-3 text-[10px] font-black uppercase tracking-[0.3em] text-text-dim mb-8">
            <a href={getRelativeUrl("/")} class="hover:text-white transition-colors">Home</a>
            <span class="text-border-muted">/</span>
            <span class="text-white">Eco System</span>
        </nav>
        <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-8xl font-black mb-8 tracking-tighter text-text-main leading-[1.05] uppercase">
            The Eco <span class="text-white opacity-30">System.</span>
        </h1>
        <p class="text-lg md:text-xl text-text-muted max-w-2xl leading-relaxed font-light">
            {SITE_CONFIG.brand.name} allows you to host multiple documentation projects in a single unified hub. Explore the core engine documentation or view our project demos below.
        </p>

        <!-- Quick Search -->
        <div class="mt-10 md:mt-12 max-w-md relative group">
            <i class="fa-solid fa-magnifying-glass absolute left-4 top-1/2 -translate-y-1/2 text-[10px] text-text-ghost group-focus-within:text-white transition-colors"></i>
            <input 
                type="text" 
                id="project-search"
                placeholder="Filter ecosystem..." 
                class="w-full bg-white/[0.03] border border-border-muted rounded-xl py-3.5 md:py-4 pl-12 pr-4 text-xs text-text-main placeholder:text-text-dim focus:outline-none focus:border-white/20 focus:bg-white/[0.05] transition-all uppercase tracking-widest font-bold"
            />
        </div>
    </div>
</div>